version: '3.8'

services:
  etrans:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./uploads:/root/uploads
      - ./outputs:/root/outputs
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped
